import type { NextPage } from 'next';

import { NotFoundFeature } from '../../features/not-found/NotFoundFeature';

const Custom404Page: NextPage = () => <NotFoundFeature />;

export default Custom404Page;
